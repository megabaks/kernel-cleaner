--- kernel-cleaner/kernel-cleaner	2012-09-20 05:35:51.000000000 +0000
+++ kernel-cleaner/kernel-cleaner-no-parallel	2012-09-20 05:36:48.000000000 +0000
@@ -21,12 +21,12 @@
 SRC="/usr/src/linux"
 
 echo "Kernels....";
-KERNELS="$(parallel -j${threads} "file -F ' '" ::: /boot/* | awk '/Linux.*version/ {print $1,$9}')" && eend 0
+KERNELS="$(file -F ' ' /boot/* | awk '/Linux.*version/ {print $1,$9}')" && eend 0
 echo "Initrams...";
-INITRAMS="$(parallel -j${threads} "file -F ' '" ::: /boot/* | awk '/compressed data/ {print $1,$2}')" && eend 0
+INITRAMS="$(file -F ' ' /boot/* | awk '/compressed data/ {print $1,$2}')" && eend 0
 echo "Configs....";
-parallel -j${threads} "file -F ' '" ::: /boot/* | grep -q 'ASCII' || \
-parallel -j${threads} "file -F ' '" ::: /boot/* | grep -q 'config build file' && \
+file -F ' ' /boot/* | grep -q 'ASCII' || \
+file -F ' ' /boot/* | grep -q 'config build file' && \
 CONFIGS="$(grep -H 'Linux/' /boot/* 2>/dev/null | awk '{print $1,$3}')"
 CONFIGS="${CONFIGS//:#/}" && eend 0
 
